import { registerRootComponent } from 'expo';
import App from './App';

// Register the root component
registerRootComponent(App);

// Uncomment to use test app instead
// import TestApp from './TestApp';
// registerRootComponent(TestApp);
